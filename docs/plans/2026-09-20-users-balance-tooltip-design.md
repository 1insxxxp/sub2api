# 用户管理余额提示设计

## 目标

用户管理表格中，余额按钮的说明提示不能覆盖表头或相邻行的数据；余额仍可点击打开充值记录。

## 方案

移除余额单元格内通过 `group-hover` 显示的绝对定位黑色浮层，保留余额按钮现有点击处理，并在按钮上使用 `aria-label` 暴露“点击查看充值记录”的语义。这样不会改变业务流程，也不会让提示参与表格布局或遮挡数据。

## 验证

为 `UsersView` 增加回归测试，确认余额按钮存在、保留无障碍标签且不渲染 hover 浮层；随后运行该测试和前端类型/构建检查，并在本地用户管理页确认第一行与横向滚动状态下数据可见。
